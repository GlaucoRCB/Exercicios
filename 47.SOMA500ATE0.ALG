algoritmo "Soma500ate0"
var
   soma,contagem:inteiro
inicio
   contagem <- 500
   enquanto (contagem >=0 ) faca
      escreva(contagem,"+")
      soma <- soma + contagem
      contagem <- contagem - 50
   fimenquanto
   Escreva("A soma dos números é igual:",soma)
fimalgoritmo